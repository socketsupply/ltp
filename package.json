{
  "name": "ltp",
  "version": "1.0.0",
  "description": "A schemafull binary format, designed for inplace reads, like a simpler version of captnproto",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "node test/generate.js > test/build/ltp.c && clang -nostdlib --target=wasm32 test/build/ltp.c -Wl,--no-entry -Wl,--export-all -o test/build/ltp.wasm",
    "test": "node test/index.js && node test/wasm.js && node test/any.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "varstruct": "^6.1.3"
  }
}
